{
  "name": "football-squares-tracker",
  "version": "1.0.0",
  "description": "Track your football squares and see what you need to win",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "node server/index.js",
    "client": "cd client && npm run dev",
    "build": "cd client && npm run build",
    "start": "cross-env NODE_ENV=production node server/index.js",
    "test": "node --test server/gameLogic.test.js server/llmService.test.js server/nflService.test.js server/authService.test.js server/analyticsService.test.js server/googleAuth.test.js",
    "install:all": "npm install && cd client && npm install"
  },
  "dependencies": {
    "@vercel/postgres": "^0.10.0",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "uuid": "^9.0.0"
  },
  "devDependencies": {
    "concurrently": "^8.2.0",
    "cross-env": "^10.1.0"
  }
}
